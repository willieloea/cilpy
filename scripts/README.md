`cleac_caches.py` removes Python cache files and directories.  
`cleac_output_files.py` removes output files that result from using the library.  
For these files to perform as they should, run
```bash
python3 scripts/<file_name>.py
```
in the root directory of this repository.